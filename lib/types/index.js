/**
 * Session-pin plugin, node half. The package ships browser behavior only; the
 * Node half exists so the Loader sees a complete plugin and contributes nothing.
 */
/** Provides no host-side behavior. */
export function apply() { }
//# sourceMappingURL=index.js.map